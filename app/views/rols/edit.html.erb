<h1>Editing rol</h1>

<% form_for(@rol) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @rol %> |
<%= link_to 'Back', rols_path %>